{
    "title": "Task",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "The unique ID of the task"
        },
        "title": {
            "type": "string",
            "description": "The title of the task"
        },
        "description": {
            "type": "string",
            "description": "The description of the task"
        },
        "categoryId": {
            "type": "integer",
            "description": "The category Id of the task"
        },
        "priorityId": {
            "type": "integer",
            "description": "The priority Id of the task"    
        },
        "dueDate": {
            "type": "string",
            "format": "date",
            "description": "The due date of the task in YYYY-MM-DD format"
        },
        "status": {
            "type": "string",
            "enum": ["OPEN", "IN_PROGRESS", "POSTPONED", "COMPLETED", "DELAYED"],
            "description": "The status of the task"
        }
    },
    "additionalProperties": false
}